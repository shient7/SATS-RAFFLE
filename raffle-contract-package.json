{
  "name": "opnet-raffle-contract",
  "version": "1.0.0",
  "description": "OPNet Bitcoin Raffle/Lottery Smart Contract",
  "scripts": {
    "build": "asc Raffle.ts --outFile build/Raffle.wasm --optimize --runtime stub --exportRuntime",
    "build:debug": "asc Raffle.ts --outFile build/Raffle.wasm --runtime stub --exportRuntime",
    "test": "echo \"Run tests with OPNet test framework\""
  },
  "dependencies": {
    "@btc-vision/btc-runtime": "latest",
    "as-bignum": "^0.2.0"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.0"
  }
}
